import fetchBarbersByService from './fetchBarbersByService'
import fetchBarberAvailableHours from './fetchBarberAvailableHours'
import createBarber from './createBarber'
import getBarberByEmail from './getBarberByEmail'

export default {
    fetchBarbersByService,
    fetchBarberAvailableHours,
    createBarber,
    getBarberByEmail,
}
